# LinguaLearn - Python Dependencies
# Run: pip install -r requirements.txt

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# WebSocket support
websockets>=12.0

# Database — IMPORTANT: aiosqlite for true async I/O (fixes Bug #9)
aiosqlite>=0.19.0

# Configuration
pyyaml>=6.0.1

# HTTP Client (used for thumbnail caching)
aiohttp>=3.9.0

# YouTube subtitle & info extraction
# Must be kept up to date — YouTube changes frequently
yt-dlp>=2024.1.0

# Natural neural text-to-speech (free, via Microsoft Edge — no API key, no ffmpeg)
# Powers human-like "Read Aloud" and word pronunciation.
edge-tts>=6.1.0

# ─────────────────────────────────────────────
# OPTIONAL: Local Speech-to-Text (Whisper)
# Uncomment to enable offline transcription fallback.
# Requires ~500MB download for 'base' model.
# On Termux: also install ffmpeg (pkg install ffmpeg)
# ─────────────────────────────────────────────
# faster-whisper>=1.0.0
# numpy>=1.24.0
