/**
 * Root Next.js page — delegates entirely to _app.tsx / Zustand routing.
 */

import React from 'react';
import type { NextPage } from 'next';

const Home: NextPage = () => null;
export default Home;
